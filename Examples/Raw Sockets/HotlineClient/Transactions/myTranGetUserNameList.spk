Class TransactionGetUserNameList extends Transaction
{
	Function __Construct( HLSocket $H )
	{
		$HLSocket = $H;
		$Parameters = new Parameters();
		my $Data = $Parameters->GetData();
		SetFlags(0);
		SetIsReply(0);
		SetType(myTran->$GetUserNameList);
		SetID($H->GetUniqueID());
		SetErrorCode(0);
		SetData($Data);
		SetTotalSize(BinaryLen($Data));
		SetDataSize(BinaryLen($Data));
	}
}